# Backend Service

FastAPI-based backend for the Telegram school marketplace mini app. Handles user onboarding, listings, payments, and Telegram integration.

## Quickstart

```bash
poetry install
poetry run uvicorn app.main:app --reload
```

## Environment

Copy `.env.example` to `.env` and adjust as needed.
